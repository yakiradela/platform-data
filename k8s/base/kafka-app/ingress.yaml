apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: kafka-app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kafka-app-service
                port:
                  number: 80
